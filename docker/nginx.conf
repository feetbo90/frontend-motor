server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Allow large payloads if uploads ever go through the frontend proxy.
    client_max_body_size 20M;
}
